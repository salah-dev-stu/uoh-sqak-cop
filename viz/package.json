{
  "name": "cipherchase-viz",
  "private": true,
  "type": "module",
  "note": "Three.js vendored under viz/vendor — no build step. Serve via: uv run python scripts/viz_server.py",
  "scripts": { "test": "node --test test/" }
}
